server:
  port: 10355   # Config 서버 포트

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/nhnacademy-be11-bookscamp/config-repo.git
          default-label: main
          clone-on-start: true
